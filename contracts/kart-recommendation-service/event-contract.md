---
doc_type: event-contract
service: kart-recommendation-service
status: approved
generated_by: event-design-agent
source: docs/services/kart-recommendation-service/ddd-model.md, docs/services/kart-recommendation-service/design-decisions.md, docs/services/kart-recommendation-service/architecture.md, docs/services/kart-recommendation-service/database-design.md, docs/services/kart-recommendation-service/edge-cases.md, docs/services/kart-order-service/event-contract.md, docs/services/kart-product-service/event-contract.md
---

# Event Contract: kart-recommendation-service

Two transports, per `design-decisions.md`'s "Event Transport per Consumed Input" decision: `OrderDelivered`/`ProductCreated` on RabbitMQ — this service publishes no events of its own (see below), so it owns no exchange, only its own DLX (`recommendation.dlx`); each is consumed by binding directly to its publisher's own exchange (`order.exchange` for `OrderDelivered`, `product.exchange` for `ProductCreated`), routing key convention `service.entity.action`, per [kart-conventions.md](../../standards/kart-conventions.md) — plus the three clickstream events on Kafka (topic `kart.recommendation.clickstream-events`, per `kart-conventions.md`'s `kart.<service>.<entity>` naming, partitioned by `userId`). Every consumer queue/dead-letter destination is this service's own, never shared with another consumer of the same event (`event-standards.md`), matching every other confirmed consumer of `OrderDelivered`/`ProductCreated`'s own per-consumer-DLQ correction.

## Published Events

**None.** Recommendation is a pure consumer/query context (requirement-spec §2/§5; BRD §5.4 lists no outbound events; confirmed unchanged through `ddd-model.md`, which publishes no domain event from any of its four aggregates). This includes the three clickstream events below — they are formalized here as this service's own consumed-event contract entries, but their publisher is the Client Event Ingestion Gateway (infrastructure, `architecture.md`'s "Clickstream Publisher Resolution"), never this service.

## Consumed Events

| Event | Routing Key / Topic | Publisher | Payload (key fields) | Retry (this service's own consumer) | DLQ (this service's own consumer) | Notes |
|---|---|---|---|---|---|---|
| `OrderDelivered` | `order.order.delivered` (RabbitMQ) | `kart-order-service` | `orderId, deliveredAt` (+ line-item SKUs resolved via Order's own payload/API per [ADR-0005](../../adr/0005-unify-order-terminal-event.md)) | 5x exponential, paged on-call | `recommendation.order-delivered.dlq` | **Correction against this service's own earlier-stage docs**, applied the same way `kart-review-service/event-contract.md` corrected its own architecture.md's pre-publisher-stage guess: `requirement-spec.md`/`edge-cases.md`/`architecture.md` describe this event's tier as "3x retry, `order.dlq`" — accurate against BRD §10's original tier at the time those docs were authored, but `kart-order-service/event-contract.md` has since elevated every event it publishes (including `OrderDelivered`) to 5x exponential/paged-on-call and split the shared `order.dlq` label into per-event DLQs (`order.order-delivered.dlq`) — see that service's own requirement-spec.md resolution #7. This table uses that later, more-authoritative source. Drives `ProductAffinity` (`ddd-model.md`) via the redelivery-safe `recentOrderIds` upsert — a lost delivery after 5 retries leaves one order's contribution to co-occurrence signal permanently missing, a degraded-signal-completeness outcome, never a correctness gap in Order's own PostgreSQL record. |
| `ProductCreated` | `product.product.created` (RabbitMQ) | `kart-product-service` | `sku, name, description, categoryId, brand, price, status, attributes` (enriched by [ADR-0018](../../adr/0018-search-catalog-signal-sourcing.md); originally `sku, attributes`) | 3x | `recommendation.product-created.dlq` | Consumer relationship confirmed by [ADR-0013](../../adr/0013-recommendation-productcreated-consumption.md). Matches Product's own publish-side tier for this event exactly. Drives `TrendingPool`'s immediate neutral-seed upsert (`ddd-model.md`, `database-design.md`) — a lost delivery leaves one new product unseeded until the next full `TrendingPool` recompute (15 min) coincidentally picks up real signal for it, or never, if it stays unpurchased/unviewed; a degraded discoverability outcome, not a correctness gap. |
| `ProductViewed` | `kart.recommendation.clickstream-events` (Kafka, partition key `userId`) | Client Event Ingestion Gateway (infrastructure — `architecture.md`) | `userId, sku, sessionId, occurredAt` | 3x (Kafka consumer-group retry with backoff) | `kart.recommendation.clickstream-events.dlq` (Kafka dead-letter topic) | **New event, formalized here** — closes requirement-spec Open Question 1. Drives `UserBehaviorProfile.viewScore` (`ddd-model.md`). |
| `ProductClicked` | `kart.recommendation.clickstream-events` (Kafka, partition key `userId`) | Client Event Ingestion Gateway (infrastructure) | `userId, sku, sourceContext, sessionId, occurredAt` | 3x | `kart.recommendation.clickstream-events.dlq` | **New event, formalized here.** Drives `UserBehaviorProfile.clickScore`. `sourceContext` (e.g. `"recommendation"`, `"search"`, `"category"`) is carried for the Analytics-based drift-monitoring mechanism (edge-cases.md) to attribute a click back to whether it originated from a served recommendation — Recommendation itself does not branch on this field today, only stores it. |
| `SearchPerformed` | `kart.recommendation.clickstream-events` (Kafka, partition key `userId`) | Client Event Ingestion Gateway (infrastructure) | `userId, query, resultSkus, occurredAt` | 3x | `kart.recommendation.clickstream-events.dlq` | **New event, formalized here.** Drives a smaller per-`resultSkus`-entry contribution to `UserBehaviorProfile` (`ddd-model.md` — weakest of the three signals, since a result the user never viewed/clicked is the weakest behavioral signal). |

## Retry-Tier Justification

- **`OrderDelivered`, 5x exponential, paged on-call:** mirrors `kart-order-service/event-contract.md`'s own published tier for this event exactly, the same posture `kart-review-service/event-contract.md` takes for its own identical consumption (matching the publisher's own stated tier rather than independently re-deriving a lower one) — Order elevated this event (along with its other lifecycle events) from BRD §10's original 3x/manual-replay tier because a stuck delivery signal blocks downstream correctness gates indefinitely (Review's verified-purchase gate; here, `ProductAffinity`'s purchase-signal completeness). Recommendation's own consequence of a lost delivery is a degraded-signal-completeness outcome (never an oversell/double-charge/financial one), but this service defers to the publisher's own criticality judgment for the retry/paging tier rather than asserting a lower one unilaterally, consistent with how it already treats `OrderDelivered`'s payload/publisher as Order's own decision to make.
- **`ProductCreated`, 3x, no paging:** matches every other confirmed consumer of this event platform-wide (`kart-product-service/event-contract.md`'s own table — Search, Analytics at the same tier). A lost delivery degrades new-product discoverability only, recoverable by the next `TrendingPool` recompute once real signal accumulates.
- **`ProductViewed`/`ProductClicked`/`SearchPerformed`, 3x (Kafka consumer-group retry), no paging:** no BRD-stated number exists for any clickstream input (requirement-spec §3's Retry/DLQ row named this as a genuine, previously-unfillable gap) — this stage's own engineering default, set to match the catalog-tier 3x convention rather than inventing an independent number, since a lost clickstream event has the same "degraded signal completeness, not a correctness gap" risk profile as `ProductCreated`'s loss: `UserBehaviorProfile`'s best-effort idempotency posture (`ddd-model.md`) already tolerates occasional loss/duplication as a low-stakes, Eventually-consistent behavioral signal. Kafka's own dead-letter mechanism is a dead-letter **topic** (`kart.recommendation.clickstream-events.dlq`), not a RabbitMQ queue — the same "every consumer gets its own DLQ, never shared" principle (`event-standards.md`) applied to Kafka's own idiom, since this is the only consumer group for this topic today (Analytics' own full fan-in, ADR-0004, reads the same topic independently and would define its own dead-letter topic for its own consumer group, not share this one).

## Sign-off

- [x] Reviewed by: Automated architecture pipeline — autonomous completion authorized by project owner
- [x] Approved
