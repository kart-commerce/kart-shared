---
doc_type: event-contract
service: kart-review-service
status: approved
generated_by: event-design-agent
source: docs/services/kart-review-service/ddd-model.md, docs/services/kart-review-service/architecture.md, docs/standards/event-standards.md, docs/standards/kart-conventions.md, docs/adr/0014-review-rating-aggregate-ownership.md, docs/adr/0018-search-catalog-signal-sourcing.md, docs/adr/0021-review-verified-purchase-order-created-dependency.md, docs/services/kart-order-service/event-contract.md, docs/services/kart-product-service/event-contract.md, docs/services/kart-search-service/event-contract.md, docs/services/kart-analytics-service/event-contract.md
---

# Event Contract: kart-review-service

Exchange: `review.exchange` (RabbitMQ topic exchange, owned by this service, per [kart-conventions.md](../../standards/kart-conventions.md)). Routing key convention: `service.entity.action`, using `review` as both domain word and entity segment — the same same-name-entity-segment precedent `kart-order-service/event-contract.md` and `kart-notification-service/event-contract.md` set for a service's own principal published stream. Every consumer queue gets its own DLQ per the reusable event standard (`event-standards.md`: "never a shared/global DLQ") — BRD §10/`architecture.md`'s simplified shared `review.dlq` label is split here into one DLQ per published event, the same expansion `kart-order-service/event-contract.md` already applied to its own BRD-simplified shared `order.dlq` label.

## Published

| Event | Routing Key | Consumer(s) | Key Fields | Retry (publish-side) | DLQ (publish-side) | Criticality Justification |
|---|---|---|---|---|---|---|
| `ReviewSubmitted` | `review.review.submitted` | `kart-product-service` (ratingSummary recalc, ADR-0014), `kart-analytics-service` (full fan-in, ADR-0004), `kart-search-service` (RatingSignal projection, ADR-0018) | `orderId`, `sku`, `rating`, `reviewId`, `userId` | 2x | `review.review-submitted.dlq` | Confirmed unchanged from `architecture.md`'s existing BRD §10 tier, restated here against this service's own actual failure-mode risk, not copied forward unexamined (`event-design-agent`'s own mandate). Fires exactly once per `reviewId`, on the transition that first sets `firstPublishedAt` — a permanently lost delivery leaves `ProductRating` (Review's own canonical aggregate, updated asynchronously off this same stream) and every downstream denormalized copy (`ratingSummary`, `RatingSignal`) undercounted by one review until the DLQ is replayed. This is exactly the "catalog/search-style" class `event-standards.md` names as tolerating a looser tier: no Order Saga step waits on it (`architecture.md`: "Review... is not a participant in the Order Saga"), no money moves, and `Review`'s own PostgreSQL write (the actual source of truth for that customer's opinion) is committed and correct regardless of whether this publish or any downstream projection ever lands. |
| `ReviewUpdated` | `review.review.updated` | `kart-product-service`, `kart-analytics-service`, `kart-search-service` | `orderId`, `sku`, `oldRating`, `newRating` | 2x | `review.review-updated.dlq` | **Confirmed at the same 2x/no-paging tier as `ReviewSubmitted` — independently justified here, not copied forward from `architecture.md`'s placeholder.** `ReviewUpdated`'s failure mode is structurally identical to `ReviewSubmitted`'s: both are single incremental adjustments to the same non-transactional, eventually-consistent rating aggregates, both consumed by the identical three-service set, and a lost delivery of either leaves those aggregates off by the same one-review margin until DLQ replay — there is no basis in `ddd-model.md`'s own stated behavior for treating an edit's rating delta as more (or less) urgent than a fresh submission's initial count. It does **not** qualify for Tier 2 (3x, the "order/fulfillment lifecycle" tier `kart-notification-service/event-contract.md` reserves for events with real, if non-blocking, customer-facing timeliness stakes): unlike e.g. `OrderConfirmed`, no customer is waiting on a timely notification tied to this event, and unlike `OrderCreated`'s elevation (ADR-0020), `ReviewUpdated` is not a seed/linchpin any other event's resolution depends on — losing it affects exactly one `(orderId, sku)` ledger entry, nothing wider. |
| `ReviewUnpublished` | `review.review.unpublished` | `kart-product-service`, `kart-analytics-service`, `kart-search-service` | `orderId`, `sku`, `rating`, `reviewId`, `userId`, `reason: {AuthorRetraction \| ModeratorTakedown}` | 2x | `review.review-unpublished.dlq` | **Also confirmed at 2x/no-paging — the same tier as its two siblings, on independent examination, not by default inheritance.** Considered and rejected an elevation to Tier 2: `ddd-model.md`'s own correctness argument for introducing this event ("without it, a retracted or taken-down review's rating would remain counted forever") is an argument for the event's *existence*, not for a higher retry budget — the actual redelivery failure mode is identical in shape to `ReviewSubmitted`'s (a single, once-only aggregate adjustment, recoverable by DLQ replay, affecting only the one `(orderId, sku)` ledger entry it targets). The `ModeratorTakedown` reason path does carry a marginally different flavor of stakes — a permanently lost decrement here means an already-rejected/abusive review's rating contribution keeps inflating a public-facing display metric indefinitely if the DLQ is never drained — but this is a moderation-*display*-integrity concern, not a money-moving or Saga-blocking one, and Review's own canonical `ProductRating`/ledger and every consumer's denormalized copy are all already accepted (ADR-0014) as eventually-consistent, DLQ-recoverable projections. No fact under Review's own control is lost or corrupted by a stuck delivery — only its downstream reflections go stale — so Tier 3 (2x, no paging) remains the correct, actually-justified tier rather than a reflexive escalation. |

Consumer lists above reflect `architecture.md`'s Dependencies table (`ReviewSubmitted`, `ReviewUpdated`) and `ddd-model.md`'s Domain events section, which reuses the identical three-consumer set for `ReviewUnpublished` — "all three need this to correctly decrement their own rating aggregates, or they'll double-count forever" (`ddd-model.md`).

## Consumed

| Event | Publisher | Own Retry/DLQ (owned by publisher, listed for traceability only) | Review's Reaction |
|---|---|---|---|
| `OrderCreated` | `kart-order-service` | 5x exponential, paged on-call, `order.order-created.dlq` (per `kart-order-service/event-contract.md`'s own approved Published table) | Upserts `VerifiedPurchaseRecord.userId`/`skus` (derived from `items`), keyed on `orderId` — per [ADR-0021](../../adr/0021-review-verified-purchase-order-created-dependency.md), since `OrderDelivered` alone carries neither field |
| `OrderDelivered` | `kart-order-service` | 5x exponential, paged on-call, `order.order-delivered.dlq` (per `kart-order-service/event-contract.md`'s own approved Published table) | Upserts `VerifiedPurchaseRecord.deliveredAt`, keyed on `orderId` — feeds `POST /reviews`'s hard eligibility gate (ADR-0005) |

**Correction against `architecture.md`'s pre-publisher-stage guess, applied the same way `kart-notification-service/event-contract.md` corrected its own routing-key guesses against later-finalized publisher contracts.** `architecture.md`'s Dependencies table describes `OrderDelivered`'s own retry/DLQ tier as "3x retry, `order.dlq`" — this was accurate against `kart-order-service`'s own BRD-inherited tier at the time `architecture.md` was authored, but `kart-order-service/event-contract.md` has since elevated every event it publishes, including `OrderDelivered`, to 5x exponential/paged-on-call and split the shared `order.dlq` label into `order.order-delivered.dlq` (requirement-spec resolution #7, cited in that service's own Retry Tier Justification section). The table above uses that later, more-authoritative source rather than `architecture.md`'s now-superseded figure; this changes no behavior on Review's own consuming side (Review's own consumer-side retry/DLQ for these two inbound events is this service's own operational concern, not fixed by this document — no consumer-side tier is asserted here beyond what is already implicit in `architecture.md`'s Boundary Rationale: a delayed `OrderCreated`/`OrderDelivered` degrades to the existing "no matching delivered order found yet, try again shortly" rejection, never a synchronous failure).

**Outstanding cross-document asymmetry, not this document's own file to fix.** [ADR-0021](../../adr/0021-review-verified-purchase-order-created-dependency.md) and `ddd-model.md` both already flag that `kart-order-service/event-contract.md`'s `OrderCreated` Consumers column (currently: Payment, Notification, Analytics) does not yet name `kart-review-service`, even though this service is now a confirmed, load-bearing consumer of that event. Restated here for visibility, not resolved here — a different, already-approved service's file, out of this stage's own remit (the same posture `kart-order-service/event-contract.md`'s own text takes toward asymmetries it finds in other services' docs it does not own).

## Naming Convention Compliance

Every published event name is `<Entity><PastTenseVerb>` (`event-standards.md`): `ReviewSubmitted`, `ReviewUpdated`, and `ReviewUnpublished` all comply directly — no compound-verb-phrase exception needed here (contrast `kart-order-service/event-contract.md`'s one accepted exception, `OrderCompensationTriggered`). Routing keys follow `service.entity.action` (`kart-conventions.md`), using `review` as both domain word and entity segment for all three, matching this service's single-published-stream-per-aggregate-family model.

**Collision check against every other service's already-authored `event-contract.md`** (`kart-order-service`, `kart-payment-service`, `kart-shipping-service`, `kart-notification-service`, `kart-delivery-tracking-service`, `kart-wishlist-service`, `kart-identity-service`, `kart-user-service`, `kart-offer-service`, `kart-admin-service`, `kart-cart-service`, `kart-category-service`, `kart-product-service`, `kart-search-service`, `kart-analytics-service`): no other service's contract *publishes* `ReviewSubmitted`, `ReviewUpdated`, or `ReviewUnpublished` — all three names are exclusively owned here. `ReviewSubmitted` and `ReviewUpdated` already appear, correctly, as *consumed* rows in `kart-product-service/event-contract.md`, `kart-search-service/event-contract.md`, and `kart-analytics-service/event-contract.md` (pending-approval); `ReviewUnpublished` appears in none of them yet — expected, since `ddd-model.md` itself already flagged this as "a new integration surface for both [Product, Search], out of scope to edit here," a flag this document confirms is still open and does not resolve (a different service's already-approved/pending file).

## Consumer-Schema Compatibility Check (Against Existing Consumers' Own Contracts)

- **`kart-product-service/event-contract.md`** — `ReviewSubmitted` row: payload `orderId, sku, rating, reviewId, userId`, routing key `review.review.submitted`, 2x/`product.review-submitted.dlq`. `ReviewUpdated` row: payload `orderId, sku, oldRating, newRating`, routing key `review.review.updated`, 2x/`product.review-updated.dlq`, explicitly noted there as "provisionally treats it the same as `ReviewSubmitted`... pending the Event Design Agent confirming it on Review's own side." **Both match this contract's schema and routing key exactly, and this contract's confirmed 2x tier matches Product's own provisional adoption — no incompatibility, no update needed on Product's side beyond dropping its own "provisional" qualifier, which is that document's own future housekeeping, not this stage's to edit.**
- **`kart-search-service/event-contract.md`** — identical result: `ReviewSubmitted` (`orderId, sku, rating, reviewId, userId`, `review.review.submitted`, 2x/`search.review-submitted.dlq`) and `ReviewUpdated` (`orderId, sku, oldRating, newRating`, `review.review.updated`, 2x/`search.review-updated.dlq`, same "provisional... pending Review's own Event Design Agent confirmation" note) both match this contract's schema, routing key, and now-confirmed tier exactly. **No incompatibility.**
- **`kart-analytics-service/event-contract.md`** (pending-approval, full fan-in per ADR-0004) — lists `ReviewSubmitted` with the coarser, pre-expansion 3-field payload (`orderId, sku, rating`) sourced from "BRD §10" directly, not from this service's own contract, and lists `ReviewUpdated`'s payload as "Not yet specified," carrying the same provisional-2x note as the other two consumers. This is **not a schema incompatibility** — Analytics' own listed subset is a strict subset of this contract's actual payload (`orderId, sku, rating, reviewId, userId`), and the platform's standard unknown-field-tolerant consumer assumption (already applied identically to `ProductCreated`/`ProductUpdated` in that same document) means Analytics can safely continue reading only the three fields it already uses once it ingests the real, five-field event. This is staleness in Analytics' own not-yet-updated table, the same class of gap that document's own text already anticipates ("each event's own publishing service's already-authored `event-contract.md`... is authoritative... not this document's [to re-decide]") — flagged here for that document's own next pass, not escalated, since no actual wire-format conflict exists.
- **`ReviewUnpublished`** appears in none of the three consumers' contracts yet, consistent with `ddd-model.md`'s own flag. No incompatibility to check yet — there is no existing consumer schema for this event to conflict with; the flag is that Product's and Search's own docs need a new consumed row added in a future pass on their side (out of scope here).

## Sign-off

- [x] Reviewed by: Automated architecture pipeline — autonomous completion authorized by project owner. The cross-document asymmetry flagged above (`kart-order-service/event-contract.md`'s `OrderCreated` Consumers column) has been corrected — Review is now listed.
- [x] Approved
